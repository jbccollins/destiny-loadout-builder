import { Box } from '@mui/material';
import { InspectingOptimizationDetailsHelp } from './Helpers';

export default function InvalidLoadoutConfiguration() {
	return (
		<Box>
			<InspectingOptimizationDetailsHelp>
				Follow the troubleshooting guide in the &quot;No Results&quot; section
				to resolve this optimization.
			</InspectingOptimizationDetailsHelp>
		</Box>
	);
}
